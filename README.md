This project is a real-time chat application built using pure Java.
It uses Sockets for communication, where:
1.The Server accepts multiple client connections.
2.Each Client can send a message.
3.The Server broadcasts that message to all connected clients.
It works like a basic WhatsApp group chat, but built entirely using Java’s networking classes.
